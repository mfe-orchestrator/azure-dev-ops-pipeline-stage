{
  "name": "microfrontend-upload",
  "version": "1.0.0",
  "description": "Azure DevOps extension for uploading microfrontend bundles",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp task.json dist/task.json && cp overview.md dist/overview.md",
    "build:complete": "tsc && esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js && cp task.json dist/task.json && cp overview.md dist/overview.md",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "azure",
    "devops",
    "microfrontend",
    "upload"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "archiver": "^7.0.1",
    "azure-pipelines-task-lib": "^5.2.2",
    "form-data": "^4.0.5",
    "node-fetch": "^3.3.2",
    "tfx-cli": "^0.22.2"
  },
  "devDependencies": {
    "@types/archiver": "^7.0.0",
    "@types/node": "^24.10.1",
    "@types/node-fetch": "^2.6.13",
    "esbuild": "^0.27.1",
    "typescript": "^5.9.3"
  }
}
